# Varco docs website

This is the source for the Varco documentation website.

```bash
npm install
npm run build
npm run dev
```

For GitHub Pages project hosting, the deploy workflow builds with `SITE_BASE_PATH=/varco`.
For local preview outside GitHub Pages, set `SITE_BASE_PATH=/` if needed.
